# family-wishlist/docker-compose.yml
version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: wishlist-backend
    restart: unless-stopped
    env_file:
      - .env # Loads FAMILY_PASSWORD_HASH, DATABASE_URL etc.
    environment:
      - FAMILY_PASSWORD_HASH=${FAMILY_PASSWORD_HASH}
      - PUID=${PUID} # User ID for file permissions
      - PGID=${PGID} # Group ID for file permissions
      - TZ=${TZ} # Timezone
      - DATABASE_URL=sqlite:///./data/wishlist.db # Path inside container is always the same
    volumes:
      - ${DB_PATH:-${LOCAL_APPS_PATH}family-wishlist/data}:/app/data  # Dynamic path based on environment
      - ${DB_PATH:-${LOCAL_APPS_PATH}family-wishlist/data}/backups:/app/data/backups  # Backups directory
      - ${DB_PATH:-${LOCAL_APPS_PATH}family-wishlist/data}/migrations:/app/app/migrations/versions
      - ${LOCAL_APPS_PATH}family-wishlist/browser_scripts:/app/app/services/browser_scripts  # Browser scripts directory
      # For development, you might mount the code:
      # - ./backend/app:/app/app
    networks:
      - backend # Internal network for app communication
    expose: # Expose port to internal network, Traefik will handle external
      - 8000
    labels:
      - com.centurylinklabs.watchtower.enable=true
      - traefik.enable=true
      # Basic routing
      - traefik.http.routers.wishlist-backend.rule=Host(`wishlist.${DOMAIN_NAME}`) && PathPrefix(`/api`)
      - traefik.http.routers.wishlist-backend.tls=true
      - traefik.http.routers.wishlist-backend.entrypoints=websecure
      - traefik.http.services.wishlist-backend.loadbalancer.server.port=8000
      # Timeouts and healthcheck
      - traefik.http.middlewares.wishlist-backend-timeouts.forwardauth.trustForwardHeader=true
      - traefik.http.routers.wishlist-backend.middlewares=wishlist-backend-timeouts
      - traefik.http.middlewares.wishlist-backend-timeouts.headers.customRequestHeaders.X-Forwarded-Proto=https
      # Add response timeouts
      - traefik.http.middlewares.wishlist-backend-timeouts.headers.customResponseHeaders.X-Gateway-Timeout=30

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: wishlist-frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      - VITE_API_BASE_URL=/api  # Use a relative URL instead of absolute URL with Docker hostname
      - ENVIRONMENT=${ENVIRONMENT:-prod}
    networks:
      - frontend
      - backend # Internal network for app communication
    # No explicit port exposure needed if only Traefik accesses it
    # expose:
    #   - "80"
    ports:
      - 8888:80 # For local testing, maps to host port 8888
    labels:
      # Traefik labels for the frontend
      - com.centurylinklabs.watchtower.enable=true
      - traefik.enable=true
      - traefik.constraint=proxy-public
#      - traefik.http.routers.open-webui.middlewares=authentik@file
      - traefik.http.routers.wishlist-frontend.rule=Host(`wishlist.${DOMAIN_NAME}`)
      - traefik.http.routers.wishlist-frontend.tls=true
      - traefik.http.routers.wishlist-frontend.entrypoints=websecure
      - traefik.http.services.wishlist-frontend.loadbalancer.server.port=80

networks:
  frontend:
    external: true # Assumes 'frontend' network already exists from your Traefik setup
  backend:
    external: true  # This ensures backend network is only accessible internally

# Development Docker Compose configuration
version: '3.8'

services:
  backend:
    build:
      context: ./backend
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/wishlist.db
      - FAMILY_PASSWORD_HASH=${FAMILY_PASSWORD_HASH}
      - FAMILY_MEMBERS_CONFIG=${FAMILY_MEMBERS_CONFIG}
      - CHRISTMAS_MONTH=${CHRISTMAS_MONTH:-12}
      - CHRISTMAS_DAY=${CHRISTMAS_DAY:-25}
      - ENVIRONMENT=${ENVIRONMENT:-prod}
    restart: unless-stopped
    
  frontend:
    build:
      context: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
    environment:
      - VITE_API_BASE_URL=/api
      - ENVIRONMENT=${ENVIRONMENT:-prod}
    restart: unless-stopped